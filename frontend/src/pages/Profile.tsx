import { Link } from "react-router-dom";
import { Edit3, Mail, MapPin, Phone } from "lucide-react";
import { useAuth } from "@/context/AuthContext";
import { pets } from "@/data/pets";
import { vaccinations } from "@/data/vaccinations";
export default function Profile() { const { user } = useAuth(); const details = [["Full name", user?.name ?? "John"], ["Email", user?.email ?? "john@example.com"], ["Phone", user?.phone ?? "+91 98765 43210"], ["Address", "42 Park Lane"], ["City", "Bengaluru"], ["Country", "India"]]; return <div className="mx-auto max-w-4xl px-6 py-16 animate-fade-in"><section className="border border-borderLight p-7 sm:p-10"><div className="flex flex-col justify-between gap-6 sm:flex-row sm:items-center"><div className="flex items-center gap-5"><div className="grid h-20 w-20 place-items-center rounded-full bg-gray-100 text-3xl font-serif">{user?.name?.slice(0, 1) ?? "J"}</div><div><h1 className="text-4xl">{user?.name ?? "John"}</h1><p className="mt-2 flex items-center gap-2 text-textSecondary"><Mail className="h-4 w-4" />{user?.email ?? "john@example.com"}</p><p className="mt-1 flex items-center gap-2 text-sm text-textSecondary"><Phone className="h-4 w-4" />{user?.phone ?? "+91 98765 43210"} · Member since {user?.memberSince ?? "July 2026"}</p></div></div><Link className="inline-flex items-center gap-2 border border-borderLight px-4 py-2 text-sm" to="/settings"><Edit3 className="h-4 w-4" />Edit profile</Link></div></section><section className="mt-12"><h2 className="text-3xl">Personal information</h2><div className="mt-6 grid gap-x-8 md:grid-cols-2">{details.map(([label, value]) => <div className="border-t border-borderLight py-4" key={label}><p className="text-xs uppercase tracking-wider text-textSecondary">{label}</p><p className="mt-2">{value}</p></div>)}</div></section><section className="mt-12"><h2 className="text-3xl">My pet statistics</h2><div className="mt-6 grid gap-4 sm:grid-cols-2 lg:grid-cols-4">{[["Pets registered", pets.length], ["Predictions", pets.flatMap((pet) => pet.predictionHistory).length], ["Vaccinations", vaccinations.length], ["Favourite animal", "Dogs"]].map(([label, value]) => <div className="border border-borderLight p-5" key={label}><p className="text-3xl">{value}</p><p className="mt-2 text-sm text-textSecondary">{label}</p></div>)}</div><p className="mt-6 flex items-center gap-2 text-sm text-textSecondary"><MapPin className="h-4 w-4" />Health information is shown from local mock data.</p></section></div>; }
