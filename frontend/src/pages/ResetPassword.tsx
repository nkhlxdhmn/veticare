import { useState } from "react"; import { Link } from "react-router-dom"; import { AuthCard } from "@/components/auth/AuthCard"; import { authService } from "@/services/auth";
export default function ResetPassword() { const [done, setDone] = useState(false); const [error, setError] = useState(""); const submit = async (event: React.FormEvent<HTMLFormElement>) => { event.preventDefault(); const data = new FormData(event.currentTarget); const password = String(data.get("password")); if (password !== data.get("confirm")) return setError("Passwords do not match."); try { await authService.resetPassword(password); setDone(true); } catch (reason) { setError(reason instanceof Error ? reason.message : "Unable to reset password."); } }; return <div className="flex min-h-[calc(100vh-72px)] items-center justify-center px-6"><AuthCard title={done ? "Password updated" : "Reset password"} description={done ? "Your mock account password has been updated." : "Choose a new secure password."}>{done ? <Link className="underline" to="/login">Back to login</Link> : <form className="space-y-5" onSubmit={submit}><input name="password" required minLength={8} type="password" placeholder="New password" className="h-11 w-full rounded-md border border-borderLight px-3" /><input name="confirm" required minLength={8} type="password" placeholder="Confirm password" className="h-11 w-full rounded-md border border-borderLight px-3" />{error && <p className="text-sm text-red-700">{error}</p>}<button className="h-11 w-full rounded-full bg-textPrimary text-white">Reset password</button></form>}</AuthCard></div>; }
