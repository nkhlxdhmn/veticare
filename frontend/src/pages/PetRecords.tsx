import { useMemo, useState } from "react";
import { Link } from "react-router-dom";
import { Edit3, Plus, Search, Trash2 } from "lucide-react";
import { pets } from "@/data/pets";
import { Button } from "@/components/ui/button";
export default function PetRecords() { const [query, setQuery] = useState(""); const filtered = useMemo(() => pets.filter((pet) => `${pet.name} ${pet.animal} ${pet.breed}`.toLowerCase().includes(query.toLowerCase())), [query]); return <div className="mx-auto max-w-[1280px] px-6 py-16 animate-fade-in md:px-12 lg:px-24"><header className="flex flex-col justify-between gap-6 sm:flex-row sm:items-end"><div><p className="text-xs font-semibold uppercase tracking-[.18em] text-textSecondary">Personal health record</p><h1 className="mt-4 text-5xl md:text-6xl">My Pets</h1><p className="mt-4 text-lg text-textSecondary">Manage all your pets in one place.</p></div><Button className="rounded-full"><Plus className="mr-2 h-4 w-4" />Add pet</Button></header><div className="mt-10 flex flex-col gap-3 md:flex-row"><label className="flex flex-1 items-center gap-3 rounded-md border border-borderLight px-4"><Search className="h-4 w-4 text-textSecondary" /><input value={query} onChange={(event) => setQuery(event.target.value)} className="h-11 w-full outline-none" placeholder="Search pets" /></label>{["All animals", "Vaccination status", "Age", "Newest"].map((label) => <select className="h-11 rounded-md border border-borderLight bg-white px-3 text-sm text-textSecondary" key={label}><option>{label}</option></select>)}</div><div className="mt-10 grid gap-6 md:grid-cols-2 xl:grid-cols-3">{filtered.map((pet) => <article key={pet.id} className="group overflow-hidden rounded-2xl border border-borderLight bg-white transition hover:-translate-y-1 hover:shadow-xl hover:shadow-black/5"><div className="aspect-[5/3] overflow-hidden"><img src={pet.photo} alt={pet.name} className="h-full w-full object-cover transition duration-500 group-hover:scale-105" /></div><div className="p-6"><div className="flex items-start justify-between"><div><h2 className="text-3xl">{pet.name}</h2><p className="mt-1 text-sm text-textSecondary">{pet.breed} · {pet.age}</p></div><span className={`rounded-full px-3 py-1 text-xs font-semibold ${pet.healthStatus === "Healthy" ? "bg-green-50 text-green-700" : "bg-amber-50 text-amber-700"}`}>{pet.healthStatus}</span></div><div className="mt-6 grid grid-cols-2 border-y border-borderLight py-4 text-sm"><p><span className="block text-textSecondary">Gender</span>{pet.gender}</p><p><span className="block text-textSecondary">Weight</span>{pet.weight}</p></div><div className="mt-5"><div className="flex justify-between text-xs text-textSecondary"><span>Vaccination progress</span><span>{pet.vaccinationStatus}%</span></div><div className="mt-2 h-1.5 overflow-hidden rounded-full bg-gray-100"><div className="h-full bg-textPrimary" style={{ width: `${pet.vaccinationStatus}%` }} /></div></div><div className="mt-6 flex gap-4 text-sm"><Link className="font-semibold underline-offset-4 hover:underline" to={`/pets/${pet.id}`}>View</Link><button className="flex items-center gap-1 text-textSecondary hover:text-textPrimary"><Edit3 className="h-3.5 w-3.5" />Edit</button><button className="ml-auto text-textSecondary hover:text-red-700" aria-label={`Delete ${pet.name}`}><Trash2 className="h-4 w-4" /></button></div></div></article>)}</div></div>; }
